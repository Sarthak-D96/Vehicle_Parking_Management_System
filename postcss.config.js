
// This is the PostCSS configuration file for the ParkEasy application.
//  It specifies the plugins that will be used to process the CSS files in the project. 
// The configuration includes Tailwind CSS for utility-first styling and Autoprefixer for adding vendor prefixes to ensure cross-browser compatibility. 
// This setup allows for efficient and effective styling of the application's user interface while maintaining a clean and organized codebase.

module.exports = {
  plugins: {


module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
